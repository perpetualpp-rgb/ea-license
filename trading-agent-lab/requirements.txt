# Core lab runs on the Python standard library only - no install needed.
#
# Optional: enable the "tradingview" data source (real BTCUSDT analysis,
# mirroring the TradingView MCP server). Without it the lab uses its built-in
# simulated price feed.
tradingview-ta>=3.3.0
